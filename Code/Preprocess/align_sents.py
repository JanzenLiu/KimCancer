### to align the shitty format of the text files ###
### i.e. they don't fill a space between period('.') and the next sentence some time.

# split tokens, unless either side is a single character only, split them.
# re.sub(r" ?, ?", ", ")

